require(["cart","goods"],function(cart,goods){
	/*
	在这个函数中，其实就可以去使用 cart 模块下定义好的方法了;
	在require中的第一个数组上，按照先后顺序读取对应名称的模块文件，
	在function中按照数组中的书写顺序，给定对应的局部变量，这些变量值
	其实就是模块下的返回值
	 */ 
	// console.log(cart);
});